{
  "template": "pure",
  "ignore": [
    "libs"
  ],
  "libraries": {
    "pdf.js": {
      "template": "custom",
      "prebuild": "git submodule update --init",
      "build": "cp -r ${SRC_DIR}/* . && nodejs make generic"
    }
  }
}
